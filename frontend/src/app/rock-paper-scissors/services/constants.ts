import {assetsPath as generalAssetsPath} from '../../services/constants';

export const assetsPath = generalAssetsPath.concat('/rock-paper-scissors');
export const victoryCriterionList = ['by_player', 'by_play', 'by_points'];
export const playModeList = ['classic', 'SMBC'];
export const modifierList = ['cannibal', 'clone', 'i'];
