import {assetsPath as generalAssetsPath} from '../../services/constants';

export const assetsPath = generalAssetsPath.concat('/dixit');
export const cardsPath = assetsPath.concat('/cards');
export const cardBackPath = assetsPath.concat('/back.png');
export const nCards = 69;
